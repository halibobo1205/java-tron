diff --git a/actuator/build.gradle b/actuator/build.gradle
index 1143dc836..38f94b4ec 100644
--- a/actuator/build.gradle
+++ b/actuator/build.gradle
@@ -33,8 +33,8 @@ test {
 
 jacocoTestReport {
     reports {
-        xml.enabled = true
-        html.enabled = true
+        xml.required = true
+        html.required = true
     }
     getExecutionData().setFrom(fileTree('../framework/build/jacoco').include("**.exec"))
     afterEvaluate {
diff --git a/build.gradle b/build.gradle
index e143ab3a9..76f86c0c1 100644
--- a/build.gradle
+++ b/build.gradle
@@ -47,7 +47,9 @@ ext.archInfo = [
 ]
 
 if (!archInfo.java.is(archInfo.requires.JavaVersion)) {
-    throw new GradleException("Java ${archInfo.requires.JavaVersion} is required for ${archInfo.name}. Detected version ${archInfo.java}")
+    // TEMP E2E VALIDATION PATCH (do not merge): allow building on this JVM despite the
+    // arch/Java pairing gate. Original behavior: hard GradleException.
+    println "WARNING: TEMP PATCH ACTIVE - Java ${archInfo.requires.JavaVersion} is required for ${archInfo.name}, detected ${archInfo.java}; continuing anyway for local archive E2E validation."
 }
 
 println "Building for architecture: ${archInfo.name}, Java version: ${archInfo.java}"
@@ -136,7 +138,7 @@ subprojects {
     }
 
     task sourcesJar(type: Jar, dependsOn: classes) {
-        classifier = "sources"
+        archiveClassifier = "sources"
         from sourceSets.main.allSource
         duplicatesStrategy = DuplicatesStrategy.INCLUDE // allow duplicates
     }
diff --git a/chainbase/build.gradle b/chainbase/build.gradle
index 1a07ff95f..1cb4924f7 100644
--- a/chainbase/build.gradle
+++ b/chainbase/build.gradle
@@ -42,8 +42,8 @@ test {
 jacocoTestReport {
     dependsOn(processResources)  // explicit_dependency
     reports {
-        xml.enabled = true
-        html.enabled = true
+        xml.required = true
+        html.required = true
     }
     getExecutionData().setFrom(fileTree('../framework/build/jacoco').include("**.exec"))
     afterEvaluate {
diff --git a/chainbase/src/main/java/org/tron/core/archive/ArchiveServiceFactory.java b/chainbase/src/main/java/org/tron/core/archive/ArchiveServiceFactory.java
index 5b0ae1771..6c1de2bef 100644
--- a/chainbase/src/main/java/org/tron/core/archive/ArchiveServiceFactory.java
+++ b/chainbase/src/main/java/org/tron/core/archive/ArchiveServiceFactory.java
@@ -78,7 +78,9 @@ public final class ArchiveServiceFactory {
       return NoopArchiveService.INSTANCE;
     }
     validateSupportedConfig(config);
-    if (!Arch.isArm64()) {
+    // TEMP E2E VALIDATION PATCH (do not merge): explicit opt-in to run the archive on
+    // non-arm64 for local validation. Original behavior: unconditional rejection.
+    if (!Arch.isArm64() && !Boolean.getBoolean("tron.archive.allowNonArm64")) {
       throw new ArchiveException("archive is not supported on this build/platform");
     }
     if (config.getTxnum() == null || !config.getTxnum().isEnable()) {
diff --git a/common/build.gradle b/common/build.gradle
index 3fc955f9a..e5b991822 100644
--- a/common/build.gradle
+++ b/common/build.gradle
@@ -44,8 +44,8 @@ dependencies {
 
 jacocoTestReport {
     reports {
-        xml.enabled = true
-        html.enabled = true
+        xml.required = true
+        html.required = true
     }
     getExecutionData().setFrom(fileTree('../framework/build/jacoco').include("**.exec"))
     afterEvaluate {
diff --git a/consensus/build.gradle b/consensus/build.gradle
index 04cc24be5..f17c25c30 100644
--- a/consensus/build.gradle
+++ b/consensus/build.gradle
@@ -32,8 +32,8 @@ test {
 
 jacocoTestReport {
     reports {
-        xml.enabled = true
-        html.enabled = true
+        xml.required = true
+        html.required = true
     }
     getExecutionData().setFrom(fileTree('../framework/build/jacoco').include("**.exec"))
     afterEvaluate {
diff --git a/crypto/build.gradle b/crypto/build.gradle
index 82814af49..dc21ad7e8 100644
--- a/crypto/build.gradle
+++ b/crypto/build.gradle
@@ -16,8 +16,8 @@ dependencies {
 
 jacocoTestReport {
     reports {
-        xml.enabled = true
-        html.enabled = true
+        xml.required = true
+        html.required = true
     }
     getExecutionData().setFrom(fileTree('../framework/build/jacoco').include("**.exec"))
     afterEvaluate {
diff --git a/framework/build.gradle b/framework/build.gradle
index 0ce33f253..2536a5c9e 100644
--- a/framework/build.gradle
+++ b/framework/build.gradle
@@ -167,16 +167,16 @@ tasks.register('testWithRocksDb', Test) {
 
 jacocoTestReport {
     reports {
-        xml.enabled true
-        csv.enabled false
-        html.destination file("${buildDir}/jacocoHtml")
+        xml.required = true
+        csv.required = false
+        html.outputLocation = file("${buildDir}/jacocoHtml")
     }
     getExecutionData().setFrom(fileTree('../framework/build/jacoco').include("**.exec"))
 }
 
 def binaryRelease(taskName, jarName, mainClass) {
     return tasks.create("${taskName}", Jar) {
-        baseName = jarName
+        archiveBaseName = jarName
         version = null
         from(sourceSets.main.output) {
             include "/**"
diff --git a/platform/src/main/java/common/org/tron/common/arch/Arch.java b/platform/src/main/java/common/org/tron/common/arch/Arch.java
index 999bb631b..3cce4371f 100644
--- a/platform/src/main/java/common/org/tron/common/arch/Arch.java
+++ b/platform/src/main/java/common/org/tron/common/arch/Arch.java
@@ -76,6 +76,11 @@ public final class Arch {
 
   public static void throwIfUnsupportedJavaVersion() {
     if ((isX86() && !isJava8()) || (isArm64() && !isJava17())) {
+      // TEMP E2E VALIDATION PATCH (do not merge): explicit opt-out for local archive E2E runs.
+      if (Boolean.getBoolean("tron.arch.skipJavaCheck")) {
+        logger.warn("TEMP PATCH ACTIVE: bypassing unsupported Java version check.\n{}", withAll());
+        return;
+      }
       logger.info(withAll());
       throw new UnsupportedOperationException(String.format(
           "Java %s is required for %s architecture. Detected version %s", isX86() ? "1.8" : "17",
diff --git a/plugins/build.gradle b/plugins/build.gradle
index 09a13a19b..4b46311c4 100644
--- a/plugins/build.gradle
+++ b/plugins/build.gradle
@@ -129,16 +129,16 @@ test {
 
 jacocoTestReport {
     reports {
-        xml.enabled true
-        csv.enabled false
-        html.destination file("${buildDir}/jacocoHtml")
+        xml.required = true
+        csv.required = false
+        html.outputLocation = file("${buildDir}/jacocoHtml")
     }
     getExecutionData().setFrom(fileTree('../framework/build/jacoco').include("**.exec"))
 }
 
 def binaryRelease(taskName, jarName, mainClass) {
     return tasks.create("${taskName}", Jar) {
-        baseName = jarName
+        archiveBaseName = jarName
         version = null
         from(sourceSets.main.output) {
             include "/**"
